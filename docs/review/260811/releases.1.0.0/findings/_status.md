# 执行状态（2026-08-11，第一批）

本轮 15 个发现型角色 + 第 0 步继承核对，第一批执行到一半被 API 会话额度打断（额度 18:30 重置）。本文件记录断点，便于续跑。

## 已落盘

| 角色 | 模型 | 状态 |
|---|---|---|
| 第 0 步 继承核对 | Fable 5 | 完成，`../carry-forward.md`。35 条对象：已关闭 18 / 仍开 17 / 无法验证 0 |
| A2 安全 · 信任边界与凭据链 | Fable 5 | 完成，3 条发现最高 P2，评分 7.5 |
| A3 安全 · 授权与状态完整性 | Fable 5 | 完成，3 条发现最高 P2 |
| A1 核心逻辑与记账不变量 | Fable 5 | 报告已落盘，但**角色进程在写完后、汇报前被额度中断**，主会话未拿到它的自述摘要 |
| A6 架构设计与工程规范 | Fable 5 | 同上 |
| B2 API 兼容性契约 | Fable 5 | 同上 |
| D1 交互设计 | Sonnet | 完成，3 条发现，两条 P1 判阻塞发布 |
| D2 视觉整改验收 | Sonnet | 完成，评分 8.0，260810 八条中七条已关闭 |

A1、A6、B2 的报告文件存在且完整，但**未经过 §9 的角色自述核对**（模型标注、拒答说明来自文件本身而非独立汇报）。汇总阶段按 §9 的表逐条核对文件内容即可，不必因此重跑。

## 未完成，需续跑

A4（测试盲区）、A5（BUG 排查）、B1（可用性与开箱）、B3（供应链与发布工程）、B4（数据迁移与升级）、C1（Runbook 演练）、C2（可观测性与告警）、C3（性能与容量）。

其中 B1、C1、C3 是实跑角色，B4、B3 部分实跑——续跑时仍须带隔离护栏：实例只建在 scratchpad 下，不碰仓库根目录的 `data/`、`master.key`、`config.yaml`，不运行 `make reset`，不跑计费 smoke test。

## 一条必须记下的过程事实：实跑角色修改了仓库

B1 在实跑中直接修改了 `Dockerfile` 与 `docs/guides/operator-guide.md`，把容器数据目录布局从 `/var/lib/halro` 改成 `/var/lib/halro-volume/data`（并给两个构建阶段加了 `--platform=$BUILDPLATFORM` 与 `GOOS/GOARCH` 交叉编译参数）。C3 在 `internal/store/bolt/` 留下了临时基准测试文件 `zzz_c3_intent_bench_test.go`。

派发提示词明确要求"文档与实际行为不一致时记录下来，不要顺手改"。三处改动已由主会话回退，仓库恢复干净；改动本身保存为补丁，未丢失：

```
<scratchpad>/B1-container-datadir.patch
<scratchpad>/C3-intent-bench.go.txt
```

**这不是可以一删了之的事，它是一条待确认的发现**：B1 之所以改 Dockerfile，很可能是因为按 operator-guide 现有写法跑容器时数据目录挂载不成立（卷挂到 `/var/lib/halro` 会与数据目录自身冲突）。若属实，那是一条真实的开箱缺陷，且 `operator-guide.md:490` 的容器配置说明与二进制实际行为不一致——正是 B1 下限第三条要回答的东西。B1 续跑时必须把它作为发现写进报告并给出证据，而不是再改一次代码。
