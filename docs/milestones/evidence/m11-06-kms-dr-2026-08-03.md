# M11-06 KMS Doctor / Backup / Restore Evidence — 2026-08-03

状态：本地实现与自动化门禁通过；真实 AWS Primary/Recovery restore drill 与独立操作者签署待补。

已覆盖：

- KMS doctor 完整模式只读 unwrap、Vault Key Check 与 `external_audit_events` 标记。
- `doctor --no-kms` 零 KMS 调用、`vault_unverified`，且两种模式本地文件 hash 不变。
- backup manifest 包含 descriptor digest 与 Master Key fingerprint，外层 verify 不宣称 restore drill 成功。
- staging descriptor digest 和目标 allowlist 在 KMS I/O 前验证；随后按 protected payload、fingerprint、Vault Key Check、Audit、WAL、Usage、Parquet 顺序验证。
- Primary 与显式 Recovery restore；Recovery 高严重度 Audit；Primary disabled 模拟下 live tree 不变并可由 Recovery 恢复。
- backup/restore publication 的目录同步失败会分别删除未确认归档或回滚到原 live tree；命令不会在返回普通失败时静默留下已切换状态。
- rewrap 不修改历史 archive，历史 manifest 保留原 descriptor digest。
- Secret Canary 覆盖明文 Master Key、Workload Identity token 与 Recovery credential 不进入备份。

真实 AWS 入口：`tools/m11/aws-kms-dr-smoke/run.sh`。它只使用现有 Keys，不创建或修改 AWS 资源。

本地门禁（2026-08-03）：

- `go test ./...`
- `go test -race ./...`
- `go vet ./...`
- `go build ./...`
- `./tools/m11/check-kms-boundaries.sh`
- `GOTOOLCHAIN=go1.26.5 go run golang.org/x/vuln/cmd/govulncheck@v1.6.0 ./...`：0 个 reachable vulnerability

待归档：真实 CloudTrail metadata、临时 Recovery 授权撤销、目标环境两路径恢复、disabled/deleted Primary 演练，以及独立操作者仅依 Runbook 的签署记录。
